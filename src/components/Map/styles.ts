// Core
import { StyleSheet, StatusBar } from "react-native";

const styles = StyleSheet.create({
  buttonClose: {
    position: "absolute",
    top: 24,
    left: 24,
    zIndex: 1000
  }
});

export default styles;